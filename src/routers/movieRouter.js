import express from "express";
import { home, movieDetail } from "../controllers/movieController";

const movieRouter = express.Router();

// create the '/' route
// create the /:id route
// create the /add route (GET + POST)

export default movieRouter;
